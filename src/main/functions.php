<?php
/**
 * Compiled source for the add-on.
 *
 * Everything in src/main is built into the .so.
 * src/tests is not compiled.
 *
 * Keep at least one public constant here. The load test reads the first
 * `const NAME = '...'` in this file and checks that PHP can see it after
 * the add-on is loaded.
 *
 * If you rename the constant, change the same name in src/tests/test.php.
 */
const CORE_BASE_VERSIONS = '1.0.0';
